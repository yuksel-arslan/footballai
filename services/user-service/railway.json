{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm --filter @football-ai/database db:generate && pnpm --filter @football-ai/database build && pnpm --filter user-service build"
  },
  "deploy": {
    "startCommand": "pnpm --filter user-service start",
    "healthcheckPath": "/health",
    "restartPolicyType": "ON_FAILURE",
    "numReplicas": 1
  }
}
