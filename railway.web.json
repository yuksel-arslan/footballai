{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm install && pnpm --filter @football-ai/database build && pnpm --filter web build"
  },
  "deploy": {
    "startCommand": "cd apps/web && cp -r public .next/standalone/apps/web/ 2>/dev/null || true && cp -r .next/static .next/standalone/apps/web/.next/ && PORT=${PORT:-3000} node .next/standalone/apps/web/server.js",
    "healthcheckPath": "/",
    "restartPolicyType": "ON_FAILURE",
    "numReplicas": 1
  }
}
